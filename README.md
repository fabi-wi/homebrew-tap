# Homebrew Tap

Homebrew tap for apps published by `fabi-wi`.

## Codex Rate Limits

```bash
brew install --cask fabi-wi/tap/codex-rate-limits
```

To upgrade later:

```bash
brew update
brew upgrade --cask codex-rate-limits
```
